<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Maui.Healthcare.Views.PatientView"
             NavigatedTo="ContentPage_NavigatedTo"
             Title="PatientView">
    <VerticalStackLayout>
        <Label 
            Text="Patient View"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <VerticalStackLayout Grid.Column="0" Spacing="10">
                <Button Text="Add Patient" Clicked="AddClicked"/>
                <Button Text="Edit Patient" Clicked="EditClicked"/>
                <Button Text="Delete Patient" Clicked="DeleteClicked"/>
                <Button Text="Create Appointment" Clicked="CreateApptClicked"/>
                <Button Text="Edit Appointment" Clicked="EditApptClicked"/>
                <Button Text="Delete Appointment" Clicked="DeleteApptClicked"/>
            </VerticalStackLayout>
            
            <Grid Grid.Column="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <ListView
                    Grid.Column="0"
                    ItemsSource="{Binding Patients}"
                    SelectedItem="{Binding SelectedPatient, Mode=TwoWay}"
                >
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <ViewCell>
                                <Label Text="{Binding Display}"/>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <ListView
                    Grid.Column="1"
                    ItemsSource="{Binding Appointments}"
                    SelectedItem="{Binding SelectedAppointment, Mode=TwoWay}"
                >
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <ViewCell>
                                <Label Text="{Binding Display}"/>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                
            </Grid>

        </Grid>

        <Button
            Text="Back Home"
            Clicked="HomeClicked"
            />
    </VerticalStackLayout>
</ContentPage>